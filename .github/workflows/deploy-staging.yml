name: Deploy — Homologação (develop)

on:
  push:
    branches: [develop]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: tay-create/transnet-operacional-v2

jobs:
  build-and-push:
    name: Build e Push da imagem Docker (staging)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Login no GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build e Push da imagem (tag staging)
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:staging

  deploy:
    name: Deploy no servidor de Homologação
    runs-on: self-hosted
    needs: build-and-push

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Escrever .env.staging
        run: |
          mkdir -p /opt/transnet/staging
          printf 'JWT_SECRET=%s\nPORT=3001\nMAX_FILE_SIZE=200mb\nNODE_ENV=production\nGENERATE_SOURCEMAP=false\nDB_USER=%s\nDB_HOST=db\nDB_NAME=transnet_staging\nDB_PASSWORD=%s\nDB_PORT=5432\nFRONTEND_URL=https://homolog.tnethub.com.br\nGMAIL_USER=%s\nGMAIL_APP_PASSWORD=%s\nADMIN_MASTER_PASSWORD=%s\n' \
            '${{ secrets.STAGING_JWT_SECRET }}' \
            '${{ secrets.STAGING_DB_USER }}' \
            '${{ secrets.STAGING_DB_PASSWORD }}' \
            '${{ secrets.GMAIL_USER }}' \
            '${{ secrets.GMAIL_APP_PASSWORD }}' \
            '${{ secrets.STAGING_ADMIN_PASSWORD }}' \
            > /opt/transnet/staging/.env.staging

      - name: Copiar docker-compose.staging.yml
        run: cp ${{ github.workspace }}/docker-compose.staging.yml /opt/transnet/staging/

      - name: Deploy via Docker Compose
        run: |
          cd /opt/transnet/staging
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          docker compose -f docker-compose.staging.yml --env-file .env.staging pull app
          docker compose -f docker-compose.staging.yml --env-file .env.staging up -d db
          docker compose -f docker-compose.staging.yml --env-file .env.staging up -d --no-deps app
          docker image prune -f
